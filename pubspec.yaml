name: naghama
description: نغمة - تطبيق حديث لإدارة وتنزيل وتشغيل الوسائط المسموح بها.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  cupertino_icons: ^1.0.8
  dio: ^5.8.0+1
  sqflite: ^2.4.2
  path: ^1.9.1
  path_provider: ^2.1.5
  just_audio: ^0.10.5
  youtube_explode_dart: ^3.1.0
  ffmpeg_kit_flutter_new: ^4.6.2
  flutter_foreground_task: ^11.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
